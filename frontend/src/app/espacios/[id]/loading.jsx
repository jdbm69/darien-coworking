// src/app/espacios/[id]/loading.jsx
// Loader SSR que se muestra mientras se resuelve la promesa de page.jsx

import Loader from "@/components/Loader";

export default function Loading() {
  return <Loader />
}
